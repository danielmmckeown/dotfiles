- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc:
    ~/.gitconfig:
    ~/.vimrc:

# - create:

- shell:
#  - [git submodule update --init --recursive, Installing submodules]
  - echo "hey you guys"
  - sleep 2
  -
    command: ./setup.homebrew.zsh
    stdout: true
    stderr: true 

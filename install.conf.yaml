- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.bashrc: 
    ~/.bash_profile: 
    ~/.zshrc: 
    ~/.gitconfig: 
    ~/.vimrc: 
    ~/.config/nvim:

# - create:

- shell:
#  - [git submodule update --init --recursive, Installing submodules]
  - echo "hey you guys"
  -
    command: ./setup.homebrew.zsh
    stdout: true
    stderr: true 
